<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.community.mapper.RoleMapper">

    <select id="listEnabledRoles" resultType="com.community.vo.RoleOptionVO">
        select id,
               role_code,
               role_name
        from sys_role
        where status = 1
        order by id asc
    </select>

    <select id="listRoleIdsByStaffId" resultType="java.lang.Long">
        select role_id
        from sys_staff_role
        where staff_id = #{staffId}
          and status = 1
        order by role_id asc
    </select>

    <select id="listRoleCodesByStaffId" resultType="java.lang.String">
        select r.role_code
        from sys_role r
                 inner join sys_staff_role er on er.role_id = r.id
        where er.staff_id = #{staffId}
          and er.status = 1
          and r.status = 1
        order by r.id asc
    </select>

    <select id="listRoleNamesByStaffId" resultType="java.lang.String">
        select r.role_name
        from sys_role r
                 inner join sys_staff_role er on er.role_id = r.id
        where er.staff_id = #{staffId}
          and er.status = 1
          and r.status = 1
        order by r.id asc
    </select>

    <delete id="deleteRolesByStaffId">
        delete from sys_staff_role
        where staff_id = #{staffId}
    </delete>

    <insert id="insertStaffRole">
        insert into sys_staff_role (staff_id, role_id, status)
        values (#{staffId}, #{roleId}, 1)
    </insert>

    <select id="countEnabledRolesByIds" resultType="int">
        select count(1)
        from sys_role
        where status = 1
          and id in
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </select>
</mapper>
