<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.community.mapper.RetailOrderItemMapper">

    <insert id="insertBatch">
        insert into ord_item (name, image, ord_id, prod_id, pack_id, spec, num, amount)
            values
        <foreach collection="retailOrderItemList" item="od" separator=",">
            (#{od.name},#{od.image},#{od.orderId},#{od.productId},#{od.productBundleId},#{od.productSpec},#{od.number},#{od.amount})
        </foreach>
    </insert>
</mapper>
