<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.community.ai.mapper.AiAfterSaleRequestMapper">

    <insert id="insert" parameterType="com.community.entity.AiAfterSaleRequest" useGeneratedKeys="true" keyProperty="id">
        insert into ai_after_sale_request
        (order_id, user_id, conversation_id, request_type, reason, description, status, create_time, update_time)
        values
        (#{orderId}, #{userId}, #{conversationId}, #{requestType}, #{reason}, #{description}, #{status}, #{createTime}, #{updateTime})
    </insert>
</mapper>
